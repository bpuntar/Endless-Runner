// Name: Brandon Apuntar
// hours spent:
//

const config = {
    parent: 'phaser-game',
    type: Phaser.WEBGL,
    width: 600,
    height: 500,
    pixelArt: true,
    physics: {
        default: "arcade",
        arcade: {
            debug: true
        }
    }
}